# Secrets and local state must never be baked into the image
.env
.env.*
!.env.example
nginx/ssl
data/
*.pem
*.key

# Ambient videos are mounted at runtime (docker-compose), not baked in
media/

# Installed fresh inside the image by `npm ci`
node_modules/

# Not needed at runtime
.git
.github
.claude
.vscode
.idea
tests/
coverage/
nginx/
docker-compose*.yml
Dockerfile
.dockerignore
*.log
.DS_Store
skills.md
xwalllogo.png
